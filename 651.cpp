//https://acmp.ru/index.asp?main=task&id_task=651

#include <iostream>
#include <vector>

using namespace std;

int main() {
    int n, m;
    cin >> n >> m;
    vector<vector<bool>> table(n, vector<bool> (m, false));
    int k;
    cin >> k;
    for (int i = 0; i < 2 * k; ++i) {
        int x, y;
        cin >> x >> y;
 }
}